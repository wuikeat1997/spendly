spring:
  datasource:
    url: jdbc:h2:mem:spendly;MODE=PostgreSQL;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: classpath:liquibase/master.xml
  mail:
    host: localhost
    port: 1025

app:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret
